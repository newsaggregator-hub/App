# Database Configuration (PlanetScale)
DATABASE_HOST=your-planetscale-host.connect.psdb.cloud
DATABASE_USERNAME=your-username
DATABASE_PASSWORD=your-password
DATABASE_NAME=your-database-name

# Example PlanetScale connection string format:
# DATABASE_URL=mysql://username:password@host/database?ssl={"rejectUnauthorized":true}

# Google Analytics (Optional)
GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Development Settings
NODE_ENV=production

# Notes:
# 1. Copy this file to .env.local for local development
# 2. Set these as environment variables in Vercel dashboard for production
# 3. Get PlanetScale credentials from your database dashboard
# 4. Replace GA_MEASUREMENT_ID in index.html with your actual Google Analytics ID
